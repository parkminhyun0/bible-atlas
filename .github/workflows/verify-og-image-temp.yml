name: Verify OG Image Temp

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporarily disabled while preparing verified OG asset"
