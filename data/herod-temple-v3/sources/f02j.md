# F02J · South Wall Station Chain — P0 REVALIDATION_REQUIRED

출처: Notion CMUX Control §8 F02J quarantine · Master Roadmap P0 override ·
STEP07B WP P0 HOLD · SSOT Registries Measurement SSOT update (2026-08-25)

---

## 상태

```
HISTORICAL_SURVEY_FRAME_CANDIDATE / REVALIDATION_REQUIRED
P0 CROSS-CUTTING REFERENCE-FRAME RISK
```

**station order 만 lock, 수치는 production authority 아님.**

```
LOCKED   : SW → Double → Triple → SE   (서→동 순서)
NOT AUTH : SW 0 → Double 100.584 → Triple 189.5856 → SE 281.0256 m
```

---

## PEF 1884 raw chain

| 항목 | raw | 상태 |
|---|---|---|
| South Wall 전체 | `922 ft` | Candidate |
| Double ← SW | `330 ft` | Candidate |
| older Triple ← SE | `300 ft` | Candidate |

## derived

| 항목 | 계산 | 값 | 상태 |
|---|---|---|---|
| Triple ← SW | `922 − 300` | `622 ft` | Candidate |
| Double ↔ older Triple | `622 − 330` | `292 ft = 89.0016 m` | Candidate |

> **폐기된 값** — 구 `621 ft` 는 **직렬화 오류**로 폐기되었다.
> Measurement Registry 에 row 로 등록하지 않으며 `622 ft` derived row 의
> Notes 에 revoked history 로만 보존한다.

## 현대 종합값

| 항목 | 값 | 상태 |
|---|---|---|
| Double ↔ Triple gate separation | `~70 m` | **Conflict** |

---

## 미해결 쟁점

`292 ft = 89.0016 m` 와 현대 `~70 m` 의 차이는 단순 반올림이 아니다. 다음이 화해되어야 한다:

1. **gate center 의 정의** — 근측 문설주 / 중심 / 원측 중 무엇을 기준으로 잰 값인가
2. **Warren station semantics** — 벽면 따라 잰 거리인가, traverse station 간 거리인가
3. **후대 gate 와 Herodian core 의 대응** — 현존 Double/Triple 파사드에는 후대 요소가 있다
4. **측량 기준점 자체**

---

## 독립 교차 자료 · 등록 후보 (직접 사용 금지)

같은 저장소의 **다른 라인**(V1)이 별개 자료·도구로 같은 문제를 독립 기록했다.

`data/herod-temple/validation/anchor-residuals.json` 원문:

> "OSM 이중문↔삼중문 간격이 67 m 인데 모델·발굴 도면은 86 m 다."

| 계통 | Double↔Triple | 출처 |
|---|---|---|
| PEF 1884 파생 | `89.0016 m` | 19세기 측량 |
| 현대 종합 | `~70 m` | 현대 연구 |
| V1 모델·발굴도면 | `86 m` | 발굴 도면 |
| V1 OSM 실측 | `67 m` | OpenStreetMap |

**서로 다른 자료·도구에서 같은 부호·같은 크기(약 19–20 m)의 불일치가 독립적으로 재현되었다.**

이는 F02J 문제가 특정 자료의 오류가 아니라 **구조적 쟁점**임을 시사한다.
STEP07E F02J reconciliation 의 입력 후보로 가치가 있으나,
현 시점에서 어느 값도 production 으로 승격하지 않는다.

---

## 금지 사항

- F02J-dependent **endpoint / array promotion** 금지
- 새 production endpoint 또는 world bind 의 **단독 근거로 사용 금지**
- 네 값(`89.0016` / `70` / `86` / `67`)을 **평균내지 않는다** (`NO AVERAGE`)
- production merge 는 **STEP07E 에서만** 수행한다

## 영향받는 하위 항목 (partial reopen)

`STEP03B` · `STEP03D` · `STEP03E` · `F-08A` · `F-08C` · `F-00A` · `F-00B` ·
`STEP07B` 의 F02J-dependent spatial subset

특히 `STEP03B` 의 과거 표현 **"F02J South Wall 281.0256 m governing"** 은 폐기되었다.
`281.0256 m = 922 ft` 는 historical exterior-length **candidate** 이며
production governing length 가 아니다.

Drawing Index v0.5 의 Royal Stoa `DV-054` 도 `F02J numeric REVALIDATION_REQUIRED` 로 강등 보존한다.

---

## 정밀도 주의

`89.0016` · `281.0256` · `100.584` · `189.5856` 의 소수 4자리는
`ft × 0.3048` 의 **산술 결과**이지 측량 정밀도가 아니다.
19세기 테이프 실측의 실제 불확실성은 피트 단위다.
